{
    "configurations": [
        {
            "type": "bun",
            "request": "attach",
            "name": "Attach to Bun"
        },
        {
            "type": "bun",
            "request": "launch",
            "name": "Debug Server",
            "program": "${workspaceFolder}/server/index.js",
            "args": ["--development", "--hot"],
            "watchMode": true
        }
    ]
}
