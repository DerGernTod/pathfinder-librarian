---
bump: minor
---

Add rich game component badges to markdown rendering via marked extensions. Dice rolls (:dice{2d6}), DCs (:dc{15}), conditions (:condition{Stunned}), traits (:trait{Dragon}), and action icons (:action{2}) now render as styled inline badges. Updated LLM prompts to use the new syntax.
